# GitHub Actions Workflow with MATLAB Actions
#
# For a general overview of GitHub Actions, see
# https://docs.github.com/en/actions
#
# For using MathWorks products in GitHub Actions, see
# https://github.com/matlab-actions/overview
#
# For details about the syntax of this file, see
# https://docs.github.com/en/actions/using-workflows/workflow-syntax-for-github-actions

# Copyright 2024 - 2026 The MathWorks, Inc.

name: CI using MATLAB

on:
  push:
    paths-ignore:
      - 'docs/**'
      - '**.md'
      - '**.png'
      - '**.svg'
      - '**.txt'
      - '**.xml'

  pull_request:
    paths-ignore:
      - 'docs/**'
      - '**.md'
      - '**.png'
      - '**.svg'
      - '**.txt'
      - '**.xml'

  schedule:
    - cron:  '30 4 * * 1' # schedule a weekly build (Monday 4:30 UTC)

jobs:

  job-check-project:
    runs-on: ubuntu-latest
    steps:

      - name: Check out repository
        uses: actions/checkout@v4
          
      - name: Setup MATLAB
        uses: matlab-actions/setup-matlab@v3
        with:
          release: R2025bu5
          cache: true
          products: |
            Simulink
            Simscape
            Simscape_Driveline
            Simscape_Fluids
            MATLAB_Test

      - name: Run tests using JAVA
        uses: matlab-actions/run-command@v3
        with:
          command: openProject(pwd); buildtool AllTest;
